[project]
name = "roofing-sales-intelligence-backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "sqlmodel",
    "pydantic",
    "pydantic-settings",
    "psycopg2-binary",
    "playwright",
    "pyyaml",
    "httpx",
    "openai",
    "pytest",
]

[tool.pytest.ini_options]
pythonpath = ["."]
